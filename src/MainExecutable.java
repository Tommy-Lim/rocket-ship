
public class MainExecutable {

	public static void main(String args[]){
		//for loop practice problems
		//ForLoopPractice.LastnameFirstnameForLoops();
		
		
		//rocket ship problem
		
		int scale =4;
		RocketShip.TopBottom(scale);
		RocketShip.HLine(scale);
		RocketShip.UpperMid(scale);
		RocketShip.LowerMid(scale);
		RocketShip.HLine(scale);
		RocketShip.LowerMid(scale);
		RocketShip.UpperMid(scale);
		RocketShip.HLine(scale);
		RocketShip.TopBottom(scale);
	}
	
	
}
